# Render.com configuration for SPA routing
# Using Web Service with serve package for proper SPA routing
services:
  - type: web
    name: creative-approach
    env: node
    buildCommand: npm ci && npm run build
    startCommand: npm run serve
    pullRequestPreviewsEnabled: true
