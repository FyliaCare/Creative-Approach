# Render.com configuration for SPA routing with serve
services:
  - type: web
    name: creative-approach
    env: node
    buildCommand: npm ci && npm run build && npm install -g serve
    startCommand: serve -s dist -l 3000 --single
    pullRequestPreviewsEnabled: true
